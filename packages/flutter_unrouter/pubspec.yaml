name: flutter_unrouter
description: Flutter adapter for Unrouter.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  ht: ^0.3.0
  oref: ^2.8.0
  unstory: ^0.1.1
  unrouter_core: ^0.1.0

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
