# Changelog

## 0.1.0

- Initial Flutter adapter package split from the Unrouter workspace.
- Added Flutter `Router` integration, nested `Outlet` rendering, `Link`, route
  scope hooks, and async data loader helpers.
