name: unrouter_example
description: Runnable examples for the unrouter package.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.8

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  unrouter: ^0.12.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
