name: nocterm_unrouter
description: Nocterm adapter for Unrouter.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  nocterm: ^0.6.0
  unstory: ^0.1.1
  unrouter_core: ^0.1.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
