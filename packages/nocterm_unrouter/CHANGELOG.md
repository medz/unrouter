# Changelog

## 0.1.0

- Initial Nocterm adapter package split from the Unrouter workspace.
- Added nested route rendering with `UnrouterHost`, `Outlet`, route scope
  helpers, and shared guard support from `unrouter_core`.
