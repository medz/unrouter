name: unrouter_core
description: Platform-agnostic routing core for Unrouter.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.10.0

resolution: workspace

dependencies:
  ht: ^0.3.0
  roux: ^0.5.0
  unstory: ^0.1.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
