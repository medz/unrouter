name: unrouter_bench
description: Differential benchmark suite for unrouter.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ^3.10.0
  flutter: '>=1.17.0'

dependencies:
  flutter:
    sdk: flutter
  unrouter:
    path: ..
  unstory: ^0.1.1
  go_router: ^17.1.0
  zenrouter: ^1.1.0
  coal: ^0.0.5
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
