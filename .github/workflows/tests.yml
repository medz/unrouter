name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test-unrouter-machine:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Flutter
        uses: subosito/flutter-action@v2.21.0
        with:
          channel: stable

      - name: Install workspace dependencies
        run: dart pub get

      - name: Analyze unrouter_machine
        run: |
          cd pub/unrouter_machine
          dart pub get
          dart analyze

      - name: Test unrouter_machine
        run: |
          cd pub/unrouter_machine
          dart test

  test-unrouter-core:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Flutter
        uses: subosito/flutter-action@v2.21.0
        with:
          channel: stable

      - name: Install workspace dependencies
        run: dart pub get

      - name: Analyze unrouter core
        run: |
          cd pub/unrouter
          dart pub get
          dart analyze

      - name: Test unrouter core
        run: |
          cd pub/unrouter
          dart test

  test-flutter-unrouter:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Flutter
        uses: subosito/flutter-action@v2.21.0
        with:
          channel: stable

      - name: Install workspace dependencies
        run: flutter pub get

      - name: Analyze flutter_unrouter
        run: |
          cd pub/flutter_unrouter
          flutter pub get
          flutter analyze

      - name: Test flutter_unrouter
        run: |
          cd pub/flutter_unrouter
          flutter test
