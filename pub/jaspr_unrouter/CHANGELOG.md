## Unreleased

### Added

- Initial Jaspr adapter package for `unrouter`.
- Inherited core `Unrouter` directly to reduce adapter forwarding layers.
- Added a complete Jaspr example app covering shell branches, guards,
  redirects, loaders, and typed route flows.
- Added functional runtime tests for server rendering, scope/controller access,
  link navigation behavior, and controller parity.
