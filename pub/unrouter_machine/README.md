# unrouter_machine

Reusable machine-kernel primitives for the unrouter ecosystem.

## Install

```bash
dart pub add unrouter_machine
```

## Provides

- generic `MachineCommand` / `MachineCommandRuntime`
- `MachineCommandDispatcher`
- bounded `MachineTransitionStore`
