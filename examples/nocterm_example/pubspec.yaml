name: nocterm_example
description: Runnable terminal example for the unrouter package.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.8

resolution: workspace

dependencies:
  nocterm: ^0.6.0
  unrouter: ^0.12.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
