name: flutter_example
description: Runnable Flutter examples for the flutter_unrouter package.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.10.8

resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  flutter_unrouter: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
