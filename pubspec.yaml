name: unrouter
description: "Declarative Flutter router with Navigator 2 pages, Navigator 1 bridge, and routingkit matching."
version: 0.1.0

repository: https://github.com/medz/unrouter

environment:
  sdk: ^3.10.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  routingkit: ^5.1.2
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
